<!DOCTYPE html>
<html lang="fa">
<head>
<meta charset="UTF-8">
<title>Daria Local Chat</title>
<style>
body {
  margin: 0;
  padding: 0;
  font-family: "Vazirmatn", sans-serif;
  background: linear-gradient(135deg, #5a60ff, #8f94fb);
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  overflow: hidden;
  direction: rtl;
}

#intro {
  position: absolute;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: #0d0d2b;
  color: white;
  font-size: 40px;
  display: flex;
  justify-content: center;
  align-items: center;
  animation: fadeOut 2s ease-in-out 2s forwards;
}

@keyframes fadeOut {
  from { opacity: 1; }
  to { opacity: 0; visibility: hidden; }
}

#chatbox {
  background: white;
  width: 450px;
  height: 550px;
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(0,0,0,0.2);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  opacity: 0;
  animation: showChat 1s ease-in-out 3s forwards;
}

@keyframes showChat {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

#messages {
  flex: 1;
  padding: 15px;
  overflow-y: auto;
  background: #f4f4ff;
}

.message {
  background: #ececff;
  border-radius: 10px;
  padding: 8px 12px;
  margin-bottom: 8px;
  word-wrap: break-word;
}

#inputArea {
  display: flex;
  padding: 10px;
  background: #fff;
  border-top: 1px solid #ccc;
}

#message {
  flex: 1;
  border: none;
  padding: 10px;
  border-radius: 10px;
  outline: none;
  background: #f1f1ff;
}

#send {
  margin-right: 8px;
  border: none;
  background: #5a60ff;
  color: white;
  padding: 10px 18px;
  border-radius: 10px;
  cursor: pointer;
  transition: 0.3s;
}
#send:hover {
  background: #474aff;
}

#users {
  position: absolute;
  left: 10px;
  top: 10px;
  background: rgba(255,255,255,0.8);
  border-radius: 10px;
  padding: 8px 12px;
  font-size: 14px;
}
</style>
</head>
<body>

<div id="intro">Daria Local</div>

<div id="chatbox">
  <div id="users">کاربران متصل: <span id="user_count">0</span></div>
  <div id="messages"></div>
  <div id="inputArea">
    <input type="text" id="message" placeholder="پیام خود را بنویسید...">
    <button id="send">ارسال</button>
  </div>
</div>

<script src="https://cdn.socket.io/4.6.1/socket.io.min.js"></script>
<script>
const socket = io();
const messages = document.getElementById('messages');
const sendBtn = document.getElementById('send');
const msgInput = document.getElementById('message');

sendBtn.onclick = () => {
  const text = msgInput.value.trim();
  if(text){
    socket.emit('send_message', text);
    msgInput.value = '';
  }
};

socket.on('receive_message', msg => {
  const div = document.createElement('div');
  div.classList.add('message');
  div.textContent = msg;
  messages.appendChild(div);
  messages.scrollTop = messages.scrollHeight;
});

socket.on('user_list', list => {
  document.getElementById('user_count').textContent = list.length;
});
</script>

</body>
</html>
